# Админ панель редактирования промптов

## 📋 Краткое описание

Добавлена функциональность управления промптами AI прямо из админ панели (`/spidoznie-kozyavki`).

Администратор может:
- ✅ Просматривать все промпты
- ✅ Фильтровать по модели и интенсивности
- ✅ Редактировать промпты в реальном времени
- ✅ Сохранять изменения в БД
- ✅ Видеть превью промпта

---

## 🗄️ Таблица БД `prompts`

```sql
CREATE TABLE prompts (
  id BIGSERIAL PRIMARY KEY,
  created_at TIMESTAMP,
  updated_at TIMESTAMP,
  
  model TEXT,          -- bytedance, nanobana, salsa
  intensity TEXT,      -- pretty, hot, salsa
  environment TEXT,    -- original, home, bathtub, bedroom, office (NULL для pretty)
  
  prompt_text TEXT,    -- Сам промпт
  
  UNIQUE(model, intensity, environment)
);
```

---

## 🔧 Установка

### 1. Применить миграцию

Выполнить SQL из `migrate_prompts.sql` в Supabase:

```sql
-- Создать таблицу
CREATE TABLE IF NOT EXISTS prompts (...);

-- Вставить дефолтные промпты
INSERT INTO prompts (...) VALUES (...);
```

### 2. Проверить

В админ панели `/spidoznie-kozyavki` должна появиться секция "🤖 Редактирование промптов"

---

## 📝 Как редактировать промпты

### Через интерфейс

1. Перейти на `/spidoznie-kozyavki`
2. Войти с админ кредентшалами
3. Прокрутить до секции "🤖 Редактирование промптов"
4. Выбрать модель и интенсивность
5. Нажать кнопку ✏️ "Редактировать"
6. Отредактировать промпт в модальном окне
7. Нажать "💾 Сохранить"

### Прямо в БД

```sql
UPDATE prompts
SET prompt_text = 'Новый промпт...'
WHERE model = 'bytedance' 
  AND intensity = 'pretty';
```

---

## 🔄 Как работает

### Получение промптов

Когда пользователь запускает генерацию:

1. **API** (`/api/agent` или `/api/goon-ai`) получает `model`, `intensity`, `environment`
2. **Запрашивает** промпт из таблицы `prompts`
3. **Если найден** - используется промпт из БД
4. **Если не найден** - используется встроенный дефолтный промпт

```typescript
// В api/agent/route.ts
const { data: promptData } = await supabase
  .from("prompts")
  .select("prompt_text")
  .eq("model", model)
  .eq("intensity", intensity)
  .eq("environment", environment)
  .single();

const prompt = promptData?.prompt_text || fallbackPrompt;
```

---

## 📚 Структура файлов

```
app/
├── spidoznie-kozyavki/page.tsx
│   └── Добавлена логика редактирования промптов
├── api/
│   ├── admin/prompts/route.ts (НОВЫЙ)
│   │   ├── GET - получить все промпты
│   │   └── POST - обновить промпт
│   ├── agent/route.ts (обновлено)
│   │   └── Теперь получает промпты из БД
│   └── goon-ai/route.ts (обновлено)
│       └── Теперь получает промпты из БД
migrate_prompts.sql (НОВЫЙ)
└── Содержит SQL для создания таблицы
```

---

## 🎯 API Endpoints

### GET `/api/admin/prompts`

Получить все промпты

```bash
curl https://yourapp.com/api/admin/prompts
```

**Response:**
```json
{
  "prompts": [
    {
      "id": 1,
      "model": "bytedance",
      "intensity": "pretty",
      "environment": null,
      "prompt_text": "Make this woman...",
      "created_at": "2026-01-16T...",
      "updated_at": "2026-01-16T..."
    }
  ]
}
```

### POST `/api/admin/prompts`

Обновить/создать промпт

```bash
curl -X POST https://yourapp.com/api/admin/prompts \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer your_token" \
  -d '{
    "action": "update-prompt",
    "model": "bytedance",
    "intensity": "hot",
    "environment": "bedroom",
    "prompt_text": "New prompt text..."
  }'
```

**Response:**
```json
{
  "success": true,
  "prompt": { ... },
  "message": "Промпт успешно обновлен"
}
```

---

## 🔐 Безопасность

⚠️ **Текущая реализация**: Базовая проверка наличия Auth header

**Рекомендации для продакшена:**

```typescript
// Добавить проверку:
1. Авторизация через Supabase
2. Проверка is_superuser
3. Rate limiting
4. Логирование всех изменений
```

---

## 📊 Примеры промптов

### ByteDance Pretty
```
Make this woman naturally more beautiful. Preserve all skin texture details...
```

### NanoBana Hot
```
Transform this woman into an absolutely stunning, enhanced version using advanced AI upscaling...
```

### Salsa
```
Transform this woman into an extremely sexy, stunningly beautiful fully naked version...
```

---

## 🐛 Troubleshooting

### Промпты не загружаются

1. Проверить, что таблица `prompts` создана
2. Проверить, что данные вставлены (`INSERT INTO prompts VALUES (...)`)
3. Проверить логи API: `/api/admin/prompts` должен вернуть данные

### Изменения не сохраняются

1. Проверить Auth header в запросе
2. Проверить логи backend'а
3. Проверить, что уникальный ключ не нарушен

### Используются встроенные промпты вместо БД

Это нормально! Если промпт не найден в БД, используется встроенный дефолтный. Проверьте в консоли браузера сообщение "📖 Промпт загружен из БД".

---

## 🚀 Следующие шаги

1. **Полная авторизация** - добавить проверку Supabase auth
2. **История изменений** - добавить таблицу для логирования
3. **Версионирование** - сохранять старые версии промптов
4. **A/B тестирование** - отслеживать результаты разных промптов
5. **Экспорт/импорт** - возможность бэкапить промпты

