# 🔧 Исправление проблем Deployment

## Проблемы и их решения

### ❌ Проблема #1: Нет pop-up регистрации при генерации
**Причина:** Modal открывается, но ссылка "/auth?mode=signup" не работает правильно на странице /auth

**Решение:** Измени ссылки на путь полной авторизации

---

### ❌ Проблема #2: Не работает регистрация нового пользователя
**Причина:** В Vercel не настроены environment variables!

**КРИТИЧНО:** 
- `SUPABASE_SERVICE_ROLE_KEY` отсутствует в production
- Это приватный ключ для регистрации пользователей через Admin API

**Решение:**
1. Открой https://vercel.com
2. Найди проект **beautify-ai**
3. Перейди в **Settings** → **Environment Variables**
4. Добавь эти переменные из вашего .env.local:
   ```
   NEXT_PUBLIC_SUPABASE_URL = [из .env.local]
   NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY = [из .env.local]
   SUPABASE_SERVICE_ROLE_KEY = [из .env.local]
   WAVESPEED_API_KEY = [из .env.local]
   ```
5. После добавления нажми **Redeploy** в Deployments
6. Жди 2-3 минуты пока перестроится

---

### ❌ Проблема #3: Не заходит в админ консоль
**Причина:** Не проблема кода - работает локально? Если не работает - проверь:

1. Убедись что используешь email: `osmanovalev33@gmail.com`
2. И пароль: `1816424Alev!@`
3. Путь адреса должен быть: `/spidoznie-kozyavki`

Если по-прежнему не работает:
- Очисти cookies браузера
- Открой в incognito окне
- Проверь консоль браузера (F12) на ошибки

---

## Инструкция step-by-step для Vercel

### Шаг 1: Перейди в Vercel Dashboard
```
https://vercel.com/dashboard
```

### Шаг 2: Выбери проект beautify-ai
Нажми на него в списке проектов

### Шаг 3: Перейди в Settings
```
Settings → Environment Variables
```

### Шаг 4: Добавь переменные
Нажми **Add New** и добавь поочередно:

| Переменная | Значение |
|-----------|----------|
| `NEXT_PUBLIC_SUPABASE_URL` | `[из вашего .env.local]` |
| `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY` | `[из вашего .env.local]` |
| `SUPABASE_SERVICE_ROLE_KEY` | `[из вашего .env.local]` |
| `WAVESPEED_API_KEY` | `[из вашего .env.local]` |

### Шаг 5: Redeploy
Перейди в **Deployments** → нажми **•••** на последнем деплое → **Redeploy**

### Шаг 6: Проверка
Когда статус будет **Ready**:
1. Открой приложение
2. Попробуй зарегистрировать новый аккаунт
3. Проверь админ консоль

---

## Проверка работает ли

После редеплоя:
✅ Регистрация работает
✅ Pop-up закрывается после клика
✅ Админ консоль доступна
✅ Генерация запускается для новых юзеров

---

**ВАЖНО:** Без `SUPABASE_SERVICE_ROLE_KEY` в Vercel регистрация не будет работать!
