# Активация NanoBana модели

## Текущий статус

- ✅ **ByteDance** - полностью работает через Wavespeed
- ✅ **NanoBana** - готовая структура, ждет активации в Wavespeed

---

## Информация о моделях

Обе модели используют **Wavespeed API**, но разные endpoints:

| Модель | Endpoint | Статус |
|--------|----------|--------|
| ByteDance | `/api/v3/bytedance/seedream-v4/edit` | ✅ Активна |
| NanoBana | `/api/v3/nanobana/edit` | ⏳ Готовка (требует активации на Wavespeed) |

---

## Шаги для активации NanoBana

### 1. Проверить доступность NanoBana в Wavespeed

Свяжитесь с **Wavespeed** и попросите активировать:
- ✅ Доступ к модели NanoBana
- ✅ Убедиться, что endpoint `/api/v3/nanobana/edit` работает

### 2. Протестировать endpoint вручную

```bash
curl -X POST https://api.wavespeed.ai/api/v3/nanobana/edit \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "images": ["https://example.com/image.jpg"],
    "prompt": "Make this beautiful",
    "enable_base64_output": false,
    "enable_sync_mode": false
  }'
```

Если ответ успешный (статус 200-201), то NanoBana готова!

### 3. Всё! Код уже готов

Когда NanoBana станет доступна в Wavespeed, код **автоматически** будет работать:

```typescript
// В коде уже добавлены оба endpoints:
const WAVESPEED_BYTEDANCE_URL = "https://api.wavespeed.ai/api/v3/bytedance/seedream-v4/edit";
const WAVESPEED_NANOBANA_URL = "https://api.wavespeed.ai/api/v3/nanobana/edit";

// Логика выбора уже реализована:
if (model === "nanobana") {
  apiUrl = WAVESPEED_NANOBANA_URL;
} else {
  apiUrl = WAVESPEED_BYTEDANCE_URL;
}
```

---

## Тестирование после активации

1. Перезагрузить приложение
2. На главной странице нажать кнопку "NanoBana" (модель AI)
3. Загрузить фото
4. Проверить консоль в браузере:
   - Должна быть строка "🚀 Используем NanoBana модель (Wavespeed)"
   - Если ошибка - проверить логи backend'а

---

## Файлы, где реализована поддержка

- [app/api/agent/route.ts](../app/api/agent/route.ts) - выбор модели и промптов

---

## Полезные ссылки

- [Wavespeed Documentation](https://wavespeed.ai/)
- [Wavespeed API Models](https://wavespeed.ai/models)

---

## FAQ

**Q: Нужно ли менять код когда NanoBana станет доступна?**  
A: Нет! Код уже полностью готов. Просто попросите Wavespeed активировать модель.

**Q: Почему NanoBana используется через Wavespeed?**  
A: Потому что Wavespeed поддерживает обе модели и имеет единый интерфейс.

**Q: Что будет, если NanoBana недоступна?**  
A: Пользователь получит ошибку от Wavespeed API с объяснением.

