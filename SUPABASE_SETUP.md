# Настройка Supabase для Beautify.AI

## 1. Создать проект на Supabase

1. Перейди на https://supabase.com
2. Нажми "Start your project"
3. Создай новый проект (выбери регион)
4. Дождись инициализации

## 2. Скопировать ключи доступа

### Project URL:
1. Перейди в Settings → General
2. В разделе "Project Details" найди **Project URL** (выглядит как `https://xxxxx.supabase.co`)
3. Скопируй весь URL в `NEXT_PUBLIC_SUPABASE_URL`

### API Keys:
1. Перейди в Settings → API Keys
2. Найди **Publishable key** → скопируй в `NEXT_PUBLIC_SUPABASE_ANON_KEY`
3. Найди **Secret keys** → строка с `sb_secret_` → скопируй в `SUPABASE_SERVICE_ROLE_KEY`

## 3. Вставить ключи в .env.local

```
NEXT_PUBLIC_SUPABASE_URL="ваш-url"
NEXT_PUBLIC_SUPABASE_ANON_KEY="ваш-anon-key"
SUPABASE_SERVICE_ROLE_KEY="ваш-service-role-key"
```

## 4. Создать таблицы в SQL Editor

1. В Supabase Dashboard перейди в SQL Editor
2. Нажми на иконку + и выбери "New Query"
3. Скопируй содержимое файла `database.sql`
4. Выполни запрос

## 5. Включить Email Auth

В Supabase Dashboard:
- Перейди в Authentication → Providers
- Email должен быть включен по умолчанию

## Экономика nippies

- 1 nippy ≈ 0.0027 USD (примерно)
- Pretty: 5 бесплатных генераций, потом 0 nippies (бесплатно)
- Hot: 1 бесплатная генерация, потом 37 nippies за генерацию (~10 центов)
- Salsa: 50 nippies за генерацию (~13.5 центов)
- Суперпользователи (администраторы) получают все бесплатно

## Иконка для nippies

🌶 (красный перец) - символизирует "горячесть" и привлекательность

## Как сделать суперпользователя

В SQL Editor выполни:
```sql
UPDATE users SET is_superuser = true WHERE email = 'твой-email@example.com';
```

## Инструкции для помошников

1. Зарегистрируйся на http://localhost:3000/auth
2. Дайте мне свой email
3. Я добавлю вас как суперпользователей в БД

После этого вам будут доступны все функции бесплатно!
